# WP-D · Historical Index Reconstitution Calendar (IDX)

> Research infrastructure for **P-A · Auction Dislocation** — the data-assembly work package that gates registration of [[HYP-PA-0001_DRAFT|HYP-PA-0001]].

**Status:** Delivered (partial coverage — see [[COVERAGE_REPORT]]) · **Owner:** Research Director / CRO · **Assembled:** 2026-07-17
**Scope:** LQ45, IDX30, IDX80 reconstitution events · **Governance standing:** none — a Work Package carries no family and gates nothing ([[RESEARCH_PROGRAM_STANDARD]] §4, PG-8). It is the single data precondition to G1 for HYP-PA-0001.

## Contents

| File | What it is |
|---|---|
| `reconstitution_events.csv` | The canonical dataset — one row per ticker-event, full provenance ([[DATA_DICTIONARY]]) |
| [[DATA_DICTIONARY]] | Field definitions, types, allowed values, validation rules |
| [[SOURCE_REGISTRY]] | Primary/secondary sources, trust ranking, conflict-resolution policy |
| [[COVERAGE_REPORT]] | Years covered, event counts, missing periods, confidence, gap analysis |
| [[READINESS_ASSESSMENT]] | Whether inputs for N / σ / MDE / power now exist (no power computed) |

## Integrity guarantees

- **No imputation.** Every event is transcribed from a named, retrieved published source with a URL and retrieval date. Reviews or indices not found in a source are recorded as **gaps**, never inferred ([[COVERAGE_REPORT]] §Gaps).
- **Reproducible.** The CSV is machine-generated by `scratchpad/build_wpd.py` (kept out of the repo) from the source records embedded there; each row is independently auditable from its `source_url`.
- **No experiment, no inference, no registration.** WP-D stops at the event inventory. Power analysis, σ estimation, and the `DRAFT → REGISTERED` transition are downstream and out of scope here.

## Headline numbers

210 ticker-events (105 ADD / 105 DELETE) across **13 reviews (= 13 P-A clusters)** · 192 distinct `(ticker, effective_date, direction)` economic events · window **2022-08-01 → 2026-05-04** · 6 of 13 reviews cross-corroborated. Consistency audit: **PASS** (no ADD/DELETE collisions, all provenance present, all within the OHLCV window). The quarterly era (2024-Q2 → 2026-Q2) is complete; only **2021-H2 and 2022-H1** remain uncovered. Primary (IDX) verification is **0** (Cloudflare-blocked, [[COVERAGE_REPORT]] §5).
