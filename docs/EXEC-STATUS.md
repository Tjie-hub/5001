# EXEC-STATUS — Engineering Dashboard

**Owner lane:** QA (EXEC-001 §13, updated daily per §9)
**Rule:** derivative only — cites manifests, ledger, and evidence paths; never the primary record of anything (EXEC-001 §14).
**Last updated:** 2026-07-30 (P0.E2.S3.T2 implemented, cold-reviewed (0 findings), and merged to master — dead `stockbit_fetcher._parse_args` deleted)

---

## 1. Program position

- **Phase:** 0 — Audit Triage (PLAN-001 §3)
- **Days into phase:** 0 (bring-up day)
- **Session counters:** n/a — Phase 0 has no session-count gate (that starts Phase 1: 10 sessions; Phase 3: ≥20 sessions)

## 2. WIP

- **Active task:** none — `P0.E2.S3.T2` cold-reviewed and merged; no P0 task branch open (ER-1: at most one active task).
- **Parallel-list tasks in flight:** none

## 3. Gate progress — Gate 0 (`gate/phase-0`)

Full checklist: `docs/evidence/P0/GATE.md`. Summary:

| Item | State |
|---|---|
| Bring-up (EXEC-001 §15) | 9/9 done — see `docs/evidence/P0/GATE.md` "Bring-up status" table |
| P0 tasks merged w/ evidence | 14/16 merged (P0.E1.S1.T1, P0.E1.S1.T2, P0.E1.S2.T1, P0.E1.S2.T2, P0.E1.S2.T3, P0.E1.S2.T4, P0.E1.S2.T5, P0.E1.S2.T6, P0.E2.S1.T1, P0.E2.S1.T2, P0.E2.S2.T1, P0.E2.S2.T2, P0.E2.S3.T1, **P0.E2.S3.T2**). P0.E1, P0.E2.S1, and P0.E2.S2 fully closed. Denominator stands at 16; remaining 2 are P0.E2.S3.T3–T4. |
| AN-8 grep-audit (zero unregistered jobs) | done, merged — `scripts/audits/an8_unregistered_jobs.py` checks all 37 scheduler-exported candidates: 37/37 clean, 0 allowlisted (T6 registered the last outstanding one, `run_vpin_backfill`). |
| VPIN block demonstrated | done — full behaviour matrix proven, `docs/evidence/P0/P0.E1.S1.T2/` |
| Absolute DB path + identity logging | done — P0.E2.S2.T1 merged (`config` resolves absolute `DB_PATH` once via `resolve_db_path()`; ~20 modules' per-module fallback/duplicate resolution deleted, `docs/evidence/P0/P0.E2.S2.T1/`); P0.E2.S2.T2 merged (`data.db.log_db_identity()` — resolved absolute path + stat-derived file identity logged once at `app.py` startup, `docs/evidence/P0/P0.E2.S2.T2/`). P0.E2.S2 fully closed. |
| Date guards live | done — P0.E2.S1.T1 merged (EOD coverage-fallback date guard, `docs/evidence/P0/P0.E2.S1.T1/`); P0.E2.S1.T2 merged (scan-loop/monitor/distribution-scan freshness guard, `docs/evidence/P0/P0.E2.S1.T2/`). P0.E2.S1 fully closed. |
| Legacy baseline declaration | pending — written after P0.E1/P0.E2 land |
| Pre-merge gate script operational | done — `scripts/pre_merge_gate.py` (see gate run below) |
| Three-lane sign-off | pending |

**Blocking items:** remaining 2 P0 tasks, both P0.E2.S3 (small severities: calendar-year-missing alarm, holiday fail-open note), in order (no forward-phase work smuggled in — ER-2). P0.E1.S1 (VPIN gate integrity), P0.E1.S2 (dead jobs/reports decision, grew to T1–T6 as each task's own investigation surfaced follow-on work), P0.E2.S1 (date guards, T1+T2), and P0.E2.S2 (DB identity, T1+T2) are all fully closed. `P0.E2.S3.T1` (`/metrics` column fix) and `P0.E2.S3.T2` (dead `_parse_args` deleted) merged 2026-07-30.

## 4. Quality state

- **Last gate-script run:** 2026-07-30 (on `master`, post-P0.E2.S3.T2-merge) — `GATE: PASS` (QG-1 full suite 1,287 passed/1 skipped/0 failed [1,283 P0.E2.S3.T1 baseline + 4 new `test_stockbit_fetcher_cli.py` tests]; QG-4 N/A; QG-9 AN-8 audit 37 checked, 37 clean, 0 allowlisted, unaffected by this task; QG-5 evidence presence checked)
- **QG stops this week:** 1 (transient, P0.E1.S1.T1 evidence-timing — see prior entry) + 1 (T4's own audit-tool test caught a self-reference detection bug before it shipped — see T4 EVIDENCE.md §"Bug caught by this task's own test suite" — fixed same session, not a DEF: no code merged in that state)
- **Cold-review findings last cycle:** P0.E2.S3.T2 — 0 findings. Root cause traced to the original audit (`Audit/PRODUCTION_ENGINE_AUDIT_2026-07-22.md` L-3) before coding: `stockbit_fetcher._parse_args` was dead (repo-wide grep, code and non-code, found only its own definition) and buggy (self-referential list comprehension); both live entry points (`main()` and `_run_flow_cmd()`) independently re-implement `--token`/`--cat` parsing inline, correctly, and never call it. Deleted verbatim (20 lines), no other lines touched. 4 new named tests in `tests/test_stockbit_fetcher_cli.py`: a regression guard confirmed to have failed against pre-fix `HEAD` (the function existed there) and pass after; 3 more exercising `main()`'s own surviving `--token`/`--cat`/positional-override parsing, none of which had prior test coverage. Cold review checked no remaining references (repo-wide grep clean), no import regressions (`sys` still used elsewhere, `ast.parse` and a real `import stockbit_fetcher` both clean), no CLI behavior change (unreachable code deleted cannot change reachable behavior; new tests independently confirm `main()`'s actual behavior), no dead code left behind (nothing else depended solely on `_parse_args`), documentation consistency (audit report correctly left unedited), and maintenance implications (net simplification, no new abstraction). Considered and rejected consolidating the two live entry points' duplicated-but-correct parsing logic — out of this task's scope (deletion only, not deduplication). 0 findings requiring code changes. Full detail: `docs/evidence/P0/P0.E2.S3.T2/EVIDENCE.md` §"Cold review". P0.E2.S3.T1 — 0 findings. Root cause traced to the original audit (`Audit/PRODUCTION_ENGINE_AUDIT_2026-07-22.md` L-1) before coding: `/metrics`'s `idx_market_risk_score` query used column names `risk_score`/`computed_at`; `market_risk_log`'s real schema (`engine/risk_alert.py`) is `score`/`created_at` — every query silently failed (caught by `app.py`'s own `_q()` exception handler), gauge permanently `NaN`. Test-first: `tests/test_metrics_endpoint.py`'s regression case was run *before* the fix and failed exactly as predicted (`NaN` instead of the inserted score), then passed after the one-line column-name correction — pre-fix/post-fix behavior empirically observed, not assumed. Considered and rejected extracting a shared "latest risk score" helper (only one call site exists repo-wide; would have been unrelated scope creep for a trivial fix). Cold review checked hidden edge cases (NULL score, `created_at` ties — both pre-existing, not worsened), regressions (grep confirmed the wrong column names had exactly one call site before the fix), duplicate logic (none introduced), documentation consistency (the audit report is a read-only historical record, correctly left unedited), platform compatibility (N/A — pure SQL), and maintenance implications (net simplification). 0 findings requiring code changes. Full detail: `docs/evidence/P0/P0.E2.S3.T1/EVIDENCE.md` §"Cold review". P0.E2.S2.T2 — 0 findings. Reviewed against the operator's explicit checklist (duplicate startup logging, logging-before-config-init, incorrect file identity, race conditions, platform assumptions, startup performance, information leakage) — every item checked and reasoned through explicitly in `docs/evidence/P0/P0.E2.S2.T2/EVIDENCE.md` §"Cold review"; the one call site (`app.py`'s `__main__` guard) and its ordering relative to `setup_logging()` were verified structurally (Python's own module-execution-order guarantee), not just observed once. `db_size_bytes`/`db_mtime` values independently cross-checked against `stat -c "size=%s mtime=%Y"` on the real DB file outside the test suite — matched exactly (155648 bytes, 2026-07-22T07:43:13 UTC). 0 findings requiring code changes. P0.E2.S2.T1 — 0 findings surviving cold review, but 1 substantive root-cause correction made *during implementation itself* (before the formal cold-review pass): this repo's real `.env` ships `DB_PATH=data/walkforward.db` (relative), so centralizing only the *default* fallback value (as the task card's literal wording suggests) would have left every module resolving a relative path in the common case (env var set) — the actual H-7 defect. Fixed with `config.resolve_db_path()`, a shared absolute-path normalizer wrapping every `os.getenv("DB_PATH", ...)` call in the codebase (both the ~20 modules centralized to `from config import DB_PATH` and the two modules — `app.py`, `data/db.py` — that keep their own `os.getenv` for verified reload-test compatibility). Formal cold-review pass (scope, hidden duplicates, config precedence, cross-platform paths, regression/migration risk) found 0 further issues — repo-wide grep confirmed no residual duplicate resolution outside the documented scope exclusions (`_archive/`, `migrations/applied/`, `scripts/verify_flow_coverage.py`'s CLI default); direct multi-module smoke import under the real `.env` confirmed all touched modules agree on one absolute path with zero circular-import risk. Full detail: `docs/evidence/P0/P0.E2.S2.T1/EVIDENCE.md` §"Cold review". P0.E2.S1.T2 — 1 Major (fixed before merge): `EVIDENCE.md` claimed `scheduled_multi_strategy_scan`'s SELL/distribution loop "shares the same ohlcv_map (no second read)" and needed no separate guard; reading the source directly showed `scan_distribution_signals()` is an independent function with its own unguarded `ohlcv_map` read — the exact H-3 failure mode this task exists to close, live on an unguarded call site. Fixed same session: guard added, 4 new end-to-end named tests, `tests/test_bearish_signal_path.py`'s stale fixed-date fixtures updated, `IMPL-DEC-007` corrected via append (`docs/EXEC-DECISIONS.md`). Full suite and gate script re-run post-fix (1,269 passed). Other three shipped guard sites independently re-derived from source, 0 further findings (`docs/evidence/P0/P0.E2.S1.T2/EVIDENCE.md` §"Cold review"). P0.E2.S1.T1 — 0 findings (PASS); scope verified (isolated to `screener/screener_jobs.py`'s coverage-fallback block + 1 new test file, no FROZEN surface, no forward-phase work); functional correctness independently re-derived by extracting and running the verbatim pre-fix inline logic against the same stale-bar input the new tests use — confirmed the pre-fix code fabricates a row from a 10-day-stale bar with no date check, post-fix it is skipped (`docs/evidence/P0/P0.E2.S1.T1/EVIDENCE.md` §"Empirical pre-fix/post-fix verification"); full suite and gate script independently re-run. T6 (prior cycle) — 0 findings (PASS); scope verified (isolated diff — only the one `add_job` registration + emptied `ALLOWLIST`, `scheduler/jobs.py` itself untouched), functional correctness re-derived from source (registered exactly once, daily 18:15 WIB, 15 min after its data source, no ordering/duplication risk since the diff is purely additive), all 3 new tests independently confirmed to fail pre-fix and pass post-fix, T4's own real-repo integration test re-verified still green with the now-empty allowlist. T5 — 1 Minor (task-card/evidence bundle said DEBT-001/DEBT-002 were already "closed"; `EXEC-DECISIONS.md` correctly said "closes on merge, not before" — fixed before merge); adversarial probing found no functional defects. T4 — 0 findings (PASS). T3 — 2 Major + 1 Minor, all resolved before merge. **Process note (T5, T6, P0.E2.S1.T1, P0.E2.S1.T2, P0.E2.S2.T1, P0.E2.S2.T2, P0.E2.S3.T1, and P0.E2.S3.T2):** all eight cold reviews occurred in the same continuous session as their respective implementations — EXEC-001 §4.1's "next working session / one sleep or run-cycle" time-gate was not literally satisfied for any of them; the operator explicitly directed and is aware of this deviation in each case.
- **Open DEF:** 0 · **Open DEBT:** 0 (DEBT-001, DEBT-002 closed on P0.E1.S2.T5 merge; DEBT-003 closed on P0.E1.S2.T6 merge) · **Open ARCH-ISS:** 0 · **Open ADR-CAND:** 0 (register empty at program start, PLAN-001 §16)

## 5. Shadow state (Phases 2–3)

N/A — no shadow comparison exists before Phase 2 (WS-I).

## 6. Ops state

- **Run statuses:** legacy scheduler only; v2 RunManifest/StageResult ships Phase 1 (P1.E5.S1)
- **Invariant checker line:** N/A — first invariant checkers ship Phase 1 (P1.E5.S4 NIGHTLY stub)
- **Watchdog age:** legacy watchdog only; v2 extension ships Phase 1 (P1.E5.S5)

## 7. Next up (critical-path order)

1. `P0.E2.S3.T3` — calendar-year-missing alarm `[L-5]`
2. `P0.E2.S3.T4` — holiday fail-open note logged `[L-4]`

*(`P0.E2.S1.T1` merged 2026-07-30; `P0.E2.S1.T2` merged 2026-07-30; `P0.E2.S2.T1` merged 2026-07-30; `P0.E2.S2.T2` merged 2026-07-30; `P0.E2.S3.T1` merged 2026-07-30; `P0.E2.S3.T2` merged 2026-07-30 — see changelog below. P0.E2.S1 and P0.E2.S2 fully closed.)*

---

*Changelog: 2026-07-23 — dashboard initialized at bring-up (EXEC-001 §15 item 8). 2026-07-23 — P0.E1.S1.T1 merged. 2026-07-23 — P0.E1.S1.T2 merged; P0.E1.S1 closed. 2026-07-23 — P0.E1.S2.T1 merged. 2026-07-23 — P0.E1.S2.T2 merged (run_foreign_snapshot deleted as superseded). 2026-07-26 — P0.E1.S2.T3 implemented on branch (register daily_fetch_report/flow_broker_report/auto_trade_status_report); evidence bundle complete, gate green. 2026-07-26 — P0.E1.S2.T3 cold-reviewed (PASS WITH COMMENTS); 2 Major + 1 Minor finding fixed (task-card wording, DEBT-001/DEBT-002 payoff task assignment); merged to master. 2026-07-26 — P0.E1.S2.T5 added via PLAN-001 §18 change control as the payoff task for DEBT-001/DEBT-002. 2026-07-26 — P0.E1.S2.T4 implemented on branch: scripts/audits/an8_unregistered_jobs.py, repository-wide AN-8 audit (37 candidates, 36 clean + 1 new finding); evidence bundle complete incl. AUDIT-REPORT.md; gate green; awaiting cold review + merge. 2026-07-26 — P0.E1.S2.T6 added via PLAN-001 §18 change control as the payoff task for DEBT-003 (run_vpin_backfill). 2026-07-30 — P0.E1.S2.T4 cold-reviewed (PASS, 0 findings) — diff, evidence, and cross-doc consistency independently re-verified in a fresh session per EXEC-001 §4 (time-gate satisfied: implementation commit was 2026-07-26); test suite and gate script re-run independently, output matched evidence bundle exactly; squash-merged to master (commit trailer `Task: P0.E1.S2.T4 [AN-8]`); branch `p0/e1-s2-t4-an8-audit` deleted. 2026-07-30 — P0.E1.S2.T5 implemented on branch `p0/e1-s2-t5-auto-trade-scope`: `auto_trade_status_report` query scoped via EXISTS join against `premover_auto_log` (mode='enforce', would_trade=1); `yesterday` cutoff fixed to `datetime.now(WIB)`. 5 new named tests, each confirmed to fail against the pre-fix code and pass after. Full suite 1,241 passed/1 skipped/0 failed. Evidence bundle complete; awaiting cold review + merge. 2026-07-30 — P0.E1.S2.T6 implemented on branch `p0/e1-s2-t6-vpin-backfill-register` (forked from `master`, independent of T5's branch): `run_vpin_backfill` registered daily mon-fri 18:15 WIB; `ALLOWLIST` entry removed from `an8_unregistered_jobs.py` (37/37 clean, 0 allowlisted). 3 new named tests. Full suite 1,239 passed/1 skipped/0 failed. Evidence bundle complete; awaiting cold review + merge. **ER-1 note:** T5 and T6 are both unmerged simultaneously — flagged as an explicit exception, not a new normal; reviewed and merged sequentially, T5 then T6. 2026-07-30 — P0.E1.S2.T5 cold-reviewed as an independent reviewer pass: scope verified (diff limited to task card, no Phase 1/FROZEN-surface touch, no unrelated files), adversarially probed with 5 edge cases beyond the shipped tests (empty DB, manual-trade starvation of `LIMIT 10`, duplicate/mixed-mode log rows, multi-day window, CLOSED-trade PnL) — all correct; 1 Minor doc-wording finding (premature "closed" language) fixed before merge; pre-fix-vs-post-fix test behavior independently re-verified, not re-read from the evidence file. Squash-merged to master; branch `p0/e1-s2-t5-auto-trade-scope` deleted. 2026-07-30 — P0.E1.S2.T6 cold-reviewed as an independent reviewer pass: scope verified (diff isolated to one `scheduler.add_job` registration + emptying `ALLOWLIST`; `scheduler/jobs.py` untouched; no Phase 1 material); adversarially checked registration uniqueness, scheduler ordering, and non-blocking-startup guarantees from source, not assumed; all 3 new tests independently confirmed to fail pre-fix (T4-only state) and pass post-fix; T4's own real-repo integration test re-verified still green. 0 findings. Reconciled against T5's already-merged `EXEC-STATUS.md`/`GATE.md`/`EXEC-DECISIONS.md`/`PLAN-001` doc changes (T6's branch had forked before T5 merged) via `git merge master` on the T6 branch, manual conflict resolution preserving both tasks' history, then squash-merged to master; branch `p0/e1-s2-t6-vpin-backfill-register` deleted. P0.E1.S2 story (dead jobs/reports decision, T1–T6) now fully closed. Next up: P0.E2 (baseline data honesty). 2026-07-30 — P0.E2.S1.T1 implemented on branch `p0/e2-s1-t1-eod-coverage-date-guard`: `screener_jobs.run_eod`'s coverage-fallback path extracted to `_coverage_fallback_row()` and given a date guard — a ticker's last OHLCV bar is only used as trade_date's fallback coverage if it's actually dated trade_date; otherwise skipped and counted in a new `stale_skipped` aggregate log line. 4 new named tests. Full suite 1,248 passed/1 skipped/0 failed. `IMPL-DEC-006` filed (stale-skip visibility + helper-extraction shape). Evidence bundle complete. 2026-07-30 — P0.E2.S1.T1 cold-reviewed as an independent reviewer pass: scope verified (diff isolated to the coverage-fallback block in one file + one new test file, no FROZEN surface, no forward-phase work); functional correctness independently re-derived by extracting master's verbatim pre-fix inline logic and running it against the same stale-bar input the new tests use — confirmed pre-fix code fabricates a row from a 10-day-stale bar, post-fix it's skipped; full suite and gate script independently re-run, matched evidence exactly. 0 findings. **Time-gate note:** as with T5/T6, this cold review occurred in the same continuous session as the implementation; operator explicitly directed single-session execution for this task. Squash-merged to master; branch `p0/e2-s1-t1-eod-coverage-date-guard` deleted. First P0.E2 task landed — 7 of 8 P0.E2 tasks remain (`S1.T2`, `S2.T1`, `S2.T2`, `S3.T1–T4`). 2026-07-30 — P0.E2.S1.T2 implemented on branch `p0/e2-s1-t2-freshness-guard`: new `engine/freshness.py` (`is_fresh()`, IDX-session-aware) wired into `scheduler/scanner.py` (`scan_momentum_signals`, `scheduled_multi_strategy_scan`) and `monitor.py` (`_check_trade`, `_evaluate_swing_trend`, `check_all_open_trades`) — a stale last bar is skipped and counted rather than evaluated as current, one aggregate warning logged per run/cycle when nonzero (same visibility shape as `IMPL-DEC-006`). 17 new named tests across `tests/test_freshness_guard.py` (9, unit), `tests/test_scanner_freshness_guard.py` (4, end-to-end on `scan_momentum_signals`), `tests/test_monitor_freshness_guard.py` (3, `_evaluate_swing_trend` + `check_all_open_trades` aggregate), and one added to the pre-existing `tests/test_monitor_kernel_exits.py`; three other pre-existing test files (`test_monitor_kernel_exits.py`'s other cases, `test_nr7_live_pipeline_e2e.py`, `test_scanner_vpin_gate.py`) had fixture dates moved from fixed 2026-06 calendar strings to today-relative offsets so their bars stay fresh under the new guard whenever the suite runs. `IMPL-DEC-007` filed: `scheduled_multi_strategy_scan`'s call site is not driven end-to-end (unlike `scan_momentum_signals`, it doesn't return right after its loop — flow-fetch/persistence/distribution-scan/watchlist passes all follow unconditionally); coverage there rests on pattern-equivalence with the tested call site, confirmed by direct inspection, not independent proof. Full suite 1,265 passed/1 skipped/0 failed (gate run required re-adding `~/.local/node/bin` to `PATH` per `IMPL-DEC-001`, which was not on `PATH` for this session by default — pre-existing environment gap, not a regression). Evidence bundle complete (`docs/evidence/P0/P0.E2.S1.T2/`); architectural-invariant check recorded in the bundle (guard sits entirely within Production Engine code, upstream of and outside `monitor.py`'s one Agent Firm touchpoint, which stays advisory-only). 2026-07-30 — P0.E2.S1.T2 cold-reviewed as an independent reviewer pass: scope verified (diff isolated to `engine/freshness.py` + the three named call sites + tests/fixtures, no FROZEN surface, no forward-phase work). 1 Major finding: `EVIDENCE.md`'s claim that `scheduled_multi_strategy_scan`'s SELL/distribution loop "shares the same ohlcv_map (no second read)" was false — `scan_distribution_signals()` is an independent function (`scanner.py:1171`, called at `scanner.py:1501`) with its own unguarded `ohlcv_map.get(ticker)` read, live to the exact H-3 failure mode this task exists to close. Fixed before merge, same session: `is_fresh()` guard added to `scan_distribution_signals()`; 4 new end-to-end named tests in `tests/test_scanner_freshness_guard.py`; `tests/test_bearish_signal_path.py`'s fixed-2026-06-05 OHLCV fixtures switched to end on the real current date (now-stale under the new guard regardless of `trade_date`); `IMPL-DEC-007` corrected via an appended update (§8 rule — entry not edited). Adversarially confirmed (via `grep` across the full call graph) no other unguarded `ohlcv_map` read exists in `scheduler/scanner.py`. Other three shipped guard sites independently re-derived from source (guard placement relative to pre-existing history-length checks, `stale`-branch short-circuiting before any persist/close/alert call): 0 further findings. Full suite re-run: 1,269 passed/1 skipped/0 failed (was 1,265; +4). Gate script re-run: PASS. **Time-gate note:** as with T5/T6/P0.E2.S1.T1, this cold review occurred in the same continuous session as the implementation; operator explicitly directed continuation. Squash-merged to master (commit trailer `Task: P0.E2.S1.T2 [H-3]`); branch `p0/e2-s1-t2-freshness-guard` deleted. P0.E2.S1 (date guards, T1+T2) now fully closed — 6 of 8 P0.E2 tasks remain (`S2.T1`, `S2.T2`, `S3.T1–T4`). 2026-07-30 — P0.E2.S2.T1 implemented directly on `master`: `config.py` gained `default_db_path()` (pure default-value computation) and `resolve_db_path(raw)` (absolute-path normalizer) as the single canonical resolution point; ~20 modules across `paper_trade.py`, all of `scheduler/`, `engine/{risk_alert,regime_filter,strategies}.py`, `screener/{brpt_filter,reversal_filter,idx_scraper,calculator,stockbit_screener}.py`, `news_filter.py`, `stockbit_fetcher.py`, `research/jobs.py` + 3 `research/studies/*.py` files, `data/loaders.py`, `scripts/{freeze_nr7_universe,pattern_backtest}.py` had their independently-duplicated (and in 8 cases, identically wrong-hardcoded) `DB_PATH` fallback deleted in favor of `from config import DB_PATH`. `app.py` and `data/db.py` verified (via `grep -rn "importlib.reload(" tests/`, not assumed) to be the only two modules ever reload-tested standalone without `config` also being reloaded — kept their own `os.getenv('DB_PATH', default_db_path())` call, now wrapped in `resolve_db_path()`. **Root cause found mid-implementation:** this repo's real `.env` ships `DB_PATH=data/walkforward.db` (relative) — `os.getenv("DB_PATH", <absolute default>)` returns the env var verbatim when set, so centralizing only the default value would not have fixed H-7's actual defect (every module silently cwd-dependent whenever the env var is present, i.e. always, in normal operation). `resolve_db_path()` added to close this; `IMPL-DEC-008` filed. 5 new named tests in `tests/test_db_path_resolution.py`, including a repo-wide grep-audit (AN-8-style) against the stale hardcoded path string and a test reproducing the real `.env`'s relative value directly. Full suite 1,274 passed/1 skipped/0 failed (was 1,269; +5). Gate: PASS. Direct multi-module smoke import (outside pytest's reload machinery, under the real `.env`) confirmed zero circular imports and all touched modules agreeing on one absolute `DB_PATH`. 2026-07-30 — P0.E2.S2.T1 cold-reviewed as an independent reviewer pass (scope: hidden duplicate resolution, startup edge cases, config precedence, cross-platform paths, regression risk, migration safety, per the operator's explicit checklist): repo-wide grep re-run post-implementation found no residual duplicate resolution outside the three documented scope exclusions (`_archive/`, `migrations/applied/patch_adaptive_strategy.py` — a dead template string, not live code, `scripts/verify_flow_coverage.py`'s CLI default — a manual, unregistered diagnostic tool); env-var precedence over the default confirmed unchanged; an already-absolute `DB_PATH` override confirmed to pass through `resolve_db_path()` unchanged (no double-joining); no schema/data migration involved, so no migration risk; `~` (tilde) non-expansion in `DB_PATH` noted as pre-existing behavior, unchanged by this task, not a regression. 0 findings requiring further changes — the one substantive fix (`resolve_db_path`) had already been made during implementation itself, before this cold-review pass, and is documented as such rather than presented as a clean first pass. No branch to delete (implemented directly on `master`, single-session, operator-directed). P0.E2.S2 (DB identity, T1+T2) now half-closed — 5 of 8 P0.E2 tasks remain (`S2.T2`, `S3.T1–T4`). 2026-07-30 — P0.E2.S2.T2 implemented directly on `master`: `data/db.py` gained `log_db_identity(db_path=None)`, defaulting to the module's own `DB_PATH` (already resolved absolute via `P0.E2.S2.T1`'s `resolve_db_path()` — no new/duplicate path resolution), logging (via this repo's structured-JSON `extra=` convention) `db_path`, `db_exists`, and — when the file exists — `db_size_bytes`, `db_mtime` (UTC ISO-8601), `db_dev`, `db_ino`; `FileNotFoundError` logs `db_path`/`db_exists=False` only, no crash. Wired into `app.py` as the first statement inside `if __name__ == "__main__":` (the repo's one true startup sequence — module-level code, including the pre-existing unconditional `setup_logging()`, always finishes first by Python's own execution order), placed before the DB-initializing calls so the log honestly reports pre-init state. 6 new named tests in `tests/test_db_identity_logging.py` covering DB-exists, DB-missing, default-to-module-DB_PATH, env-sourced DB_PATH, the relative-`.env`-value root-cause regression guard shared with T1, and a static source check proving exactly one call site inside the `__main__` guard (not at module level, where `test_health_endpoint.py`'s `importlib.reload(app_module)` would otherwise re-fire it every test run). Full suite 1,280 passed/1 skipped/0 failed (was 1,274; +6). Gate: PASS. Real startup log output captured directly from `logs/app.log` for both DB-exists and DB-missing scenarios (not just asserted in tests); `db_size_bytes`/`db_mtime` independently cross-checked against `stat` on the real DB file, matched exactly. 2026-07-30 — P0.E2.S2.T2 cold-reviewed as an independent reviewer pass against the operator's explicit 7-point checklist (duplicate startup logging, logging-before-config-init, incorrect file identity, race conditions, platform-specific assumptions, startup performance impact, information leakage) — each addressed explicitly in `docs/evidence/P0/P0.E2.S2.T2/EVIDENCE.md` §"Cold review"; no TOCTOU-class defect (the function only logs an observation, makes no decision from the stat result); `st_dev`/`st_ino` POSIX-concept caveat on non-Linux/WSL platforms documented as an accepted, non-blocking scope boundary per the task's own "whichever the architecture best supports" wording. 0 findings. No branch to delete (implemented directly on `master`, single-session, operator-directed). No `EXEC-DECISIONS.md` entry filed — no §8-classifiable event; the one design question (where "startup" means, where the function lives) was resolved by direct reference to this repo's own existing conventions, not a new pattern, and is documented in the task's own `EVIDENCE.md` rather than as a numbered decision. P0.E2.S2 (DB identity, T1+T2) now fully closed — 4 of 8 P0.E2 tasks remain, all `S3.T1–T4`. 2026-07-30 — P0.E2.S3.T1 implemented directly on `master`: `/metrics`'s `idx_market_risk_score` gauge queried `market_risk_log` for columns `risk_score`/`computed_at`; the table's real columns (`engine/risk_alert.py`'s `_ensure_table`) are `score`/`created_at` — traced to audit finding L-1 (`Audit/PRODUCTION_ENGINE_AUDIT_2026-07-22.md`) before coding. One-line fix: `SELECT score FROM market_risk_log ORDER BY created_at DESC LIMIT 1`. Extracting a shared "latest risk score" helper was considered and rejected — only one call site exists repo-wide, would have been unrelated scope creep for a trivial fix. 3 new named tests in `tests/test_metrics_endpoint.py`, written and run *before* the fix — the regression case failed exactly as the audit predicted (`NaN` instead of the inserted score), then passed after the column-name correction. Full suite 1,283 passed/1 skipped/0 failed (was 1,280; +3). Gate: PASS. Also verified directly against this sandbox's real (currently-`market_risk_log`-table-less) production DB: endpoint still returns 200/`NaN` gracefully, no regression to the "table doesn't exist yet" case. 2026-07-30 — P0.E2.S3.T1 cold-reviewed as an independent reviewer pass against the operator's explicit checklist (hidden edge cases, regressions, duplicate logic, unintended behavioral changes, documentation consistency, platform compatibility, maintenance implications) — each addressed explicitly in `docs/evidence/P0/P0.E2.S3.T1/EVIDENCE.md` §"Cold review": NULL-score and `created_at`-tie edge cases reasoned through as pre-existing, not worsened; grep confirmed the wrong column names had exactly one call site before the fix; the audit report (the only doc mentioning this gauge) is a read-only historical record, correctly left unedited. 0 findings. No branch to delete (implemented directly on `master`, single-session, operator-directed). No `EXEC-DECISIONS.md` entry — no §8-classifiable event; the "fix in place vs. extract a helper" call documented in the task's own `EVIDENCE.md`. 3 of 8 P0.E2 tasks remain, all `S3.T2–T4`. 2026-07-30 — P0.E2.S3.T2 implemented directly on `master`: `stockbit_fetcher._parse_args` (audit finding L-3) was dead code — repo-wide grep (code and non-code) found only its own definition, zero call sites; both live entry points (`main()` and `_run_flow_cmd()`) independently re-implement `--token`/`--cat` parsing inline, correctly, and never called it. Deleted verbatim (20 lines, `stockbit_fetcher.py:385-404`); no other lines touched, no imports orphaned (`sys` remains in active use elsewhere). Considered and rejected consolidating the two live entry points' duplicated-but-correct parsing — out of scope (deletion, not deduplication). 4 new named tests in `tests/test_stockbit_fetcher_cli.py`: a regression guard confirmed to have failed against pre-fix `HEAD` (`git show HEAD:stockbit_fetcher.py | grep -c _parse_args` → 1) and pass after the deletion; 3 more giving `main()`'s own surviving parsing its first test coverage (`--token`, `--cat`, explicit-tickers-override-category). Full suite 1,287 passed/1 skipped/0 failed (was 1,283; +4). Gate: PASS. 2026-07-30 — P0.E2.S3.T2 cold-reviewed as an independent reviewer pass against the operator's explicit checklist (no remaining references, no import regressions, no CLI behavior changes, no dead code left behind, documentation consistency, maintenance implications) — each addressed explicitly in `docs/evidence/P0/P0.E2.S3.T2/EVIDENCE.md` §"Cold review": unreachable code cannot change reachable behavior by construction; new tests independently confirm `main()`'s actual parsing still works; the audit report (only doc describing the bug in detail) correctly left unedited. 0 findings. No branch to delete (implemented directly on `master`, single-session, operator-directed). No `EXEC-DECISIONS.md` entry — no §8-classifiable event. 2 of 8 P0.E2 tasks remain, both `S3.T3–T4`.*
