# Edit this file to introduce tasks to be run by cron.
# 
# Each task to run has to be defined through a single line
# indicating with different fields when the task will be run
# and what command to run for the task
# 
# To define the time you can provide concrete values for
# minute (m), hour (h), day of month (dom), month (mon),
# and day of week (dow) or use '*' in these fields (for 'any').
# 
# Notice that tasks will be started based on the cron's system
# daemon's notion of time and timezones.
# 
# Output of the crontab jobs (including errors) is sent through
# email to the user the crontab file belongs to (unless redirected).
# 
# For example, you can run a backup of all your user accounts
# at 5 a.m every week with:
# 0 5 * * 1 tar -zcf /var/backups/home.tgz /home/
# 
# For more information see the manual pages of crontab(5) and cron(8)
# 
# m h  dom mon dow   command
#
# Token refresh
  40 8 * * 1-5 cd /home/tjiesar/idx-walkforward-5001 && /home/tjiesar/idx-walkforward-5001/venv/bin/python3 auto_token.py >> /home/tjiesar/idx-walkforward-5001/logs/auto_token.log 2>&1

# OHLCV fetch (pagi)
  50 8 * * 1-5 cd /home/tjiesar/idx-walkforward-5001 && /home/tjiesar/idx-walkforward-5001/venv/bin/python3 stockbit_fetcher.py --token "$(cat /home/tjiesar/idx-walkforward-5001/.stockbit_token)" >> /home/tjiesar/idx-walkforward-5001/logs/stockbit.log 2>&1

# Flow fetch (EOD) — 18:30, after APScheduler 16:05 run finishes (~16:25); avoids DB lock conflict
  30 18 * * 1-5 cd /home/tjiesar/idx-walkforward-5001 && /home/tjiesar/idx-walkforward-5001/venv/bin/python3 stockbit_fetcher.py flow --token "$(cat /home/tjiesar/idx-walkforward-5001/.stockbit_token)" >> /home/tjiesar/idx-walkforward-5001/logs/stockbit.log 2>&1

# sectors.app intel (indices + sectors + dividends) — daily 19:00 WIB after IDX close & sectors.app digest update
  0 19 * * 1-5 cd /home/tjiesar/idx-walkforward-5001 && /home/tjiesar/idx-walkforward-5001/venv/bin/python3 sectors_fetcher.py >> /home/tjiesar/idx-walkforward-5001/logs/sectors_fetcher.log 2>&1
# Signal audit (4pm WIB = 09:00 UTC)
0 9 * * 1-5 cd "/home/tjiesar/10 Projects/idx-walkforward-5001" && venv/bin/python3 audit_signals.py >> logs/audit_signals.log 2>&1
*/10 * * * * cd /home/tjiesar/idx-walkforward-5001 && venv/bin/python3 scripts/check_scheduler_heartbeat.py >> logs/heartbeat_check.log 2>&1
5 16 * * 5 cd /home/tjiesar/idx-walkforward-5001 && venv/bin/python3 -m research.cli wf-refresh >> logs/research_cli.log 2>&1
15 8 * * 1-5 cd /home/tjiesar/idx-walkforward-5001 && venv/bin/python3 -m research.cli backtest-cache >> logs/research_cli.log 2>&1
0 10 1-7 * 0 cd /home/tjiesar/idx-walkforward-5001 && venv/bin/python3 -m research.cli roller >> logs/research_cli.log 2>&1
